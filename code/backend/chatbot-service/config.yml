# Chatbot-service local config (keep this minimal)
# LLM config is controlled centrally by `backend/config.yml`.

# Server Configuration (service-level override OK)
server:
  host: "0.0.0.0"
  port: 8000
  cors_origins:
    - "http://localhost:5173"
    - "http://localhost:3000"

